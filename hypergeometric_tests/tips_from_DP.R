
#lapply(degSize, testDGEs.df) %>% assign(x=.,value=paste0("degs.", i)
#for (I in degs_list){ degsInPath <- I; }


#for(I in degsInPath){
#  degsInPath <- I
#  degs.I <- lapply(degSize, testDGEs.df)
#  degs.I.Plot <- as.data.frame(cbind(degSize,unlist(degs.I)))
#  colnames(degs.I.Plot) <- c("degSize","pval")
#}